{# templates/crud_author/AuthorAdd.html.twig #}
{% extends 'base.html.twig' %}

{% block body %}
    <h1>Add Author</h1>
    {{ form_start(form) }}
        {{ form_widget(form) }} {# This will include all fields, including the submit button #}
        {# or you can render each field manually if you prefer #}

    {{ form_end(form) }}
{% endblock %}
